## 1. 配置文件检查逻辑

- [ ] 1.1 定义配置文件路径常量（全局 2 个 + 项目级 4 个）
- [ ] 1.2 实现 JSONC 注释剥离函数（剥离 `//` 单行和 `/* */` 块注释）
- [ ] 1.3 实现插件注册检查函数：给定配置文件路径，判断 `plugin` 数组是否包含 `.opencode/plugins/opencode-lark-bridge`
- [ ] 1.4 实现全量扫描函数：遍历所有 6 个配置文件，任一已注册即返回 0（已注册）

## 2. 配置文件写入逻辑

- [ ] 2.1 实现优先级选择函数：按 `.opencode/opencode.jsonc` > `.opencode/opencode.json` > `./opencode.jsonc` > `./opencode.json` 优先级，返回第一个存在的 jsonc 文件；若无 jsonc 则返回第一个存在的 json 文件；若都不存在返回 `.opencode/opencode.jsonc`（待创建）
- [ ] 2.2 实现新建配置文件函数：创建 `.opencode/opencode.jsonc`，写入包含 `$schema` 和 `plugin` 字段的骨架
- [ ] 2.3 实现向已有配置文件添加 `plugin` 字段的逻辑（文件存在但无 `plugin` 字段）
- [ ] 2.4 实现向已有 `plugin` 数组追加插件路径的逻辑（文件存在且有 `plugin` 数组但不含本插件）
- [ ] 2.5 确保写入时保留原有配置内容和注释（用 `sed`/`awk` 做数组追加，不用 `jq` 全量重写）

## 3. 集成到 install-local.sh

- [ ] 3.1 在 `scripts/install-local.sh` 的种子配置步骤后，添加配置检查与写入调用
- [ ] 3.2 添加错误处理：检查或写入失败时输出警告并继续，不中断安装流程
- [ ] 3.3 添加日志输出：检查结果、写入目标、跳过原因

## 4. 测试验证

- [ ] 4.1 测试场景：项目级配置已注册插件 → 跳过写入
- [ ] 4.2 测试场景：全局配置已注册插件 → 跳过写入
- [ ] 4.3 测试场景：所有配置文件都未注册 → 按优先级写入 jsonc
- [ ] 4.4 测试场景：无 jsonc 但有 json → 写入 json
- [ ] 4.5 测试场景：项目级配置都不存在 → 创建 `.opencode/opencode.jsonc`
- [ ] 4.6 测试场景：配置文件格式损坏 → 输出警告并跳过
- [ ] 4.7 验证全局配置文件未被修改

## 5. 文档更新

- [ ] 5.1 更新 README.md 说明安装脚本自动注册行为
